#--------------------------------------------------------------------
# EMAIL QUEUE CONFIGURATION
#--------------------------------------------------------------------

# Enable asynchronous email sending (true = queue emails, false = send immediately)
EMAIL_ASYNC_MODE=true

# Cron endpoint security token (generate a random 32+ character string)
# Used to secure the /cron/email/process-queue endpoint
# Generate with: openssl rand -hex 32
EMAIL_CRON_TOKEN=your_secure_random_token_here_change_this

# Existing email configuration
EMAIL_METHOD=brevo
# EMAIL_METHOD=smtp

# Brevo API Key (if using Brevo)
BREVO_EMAIL_API_KEY=your_brevo_api_key_here

#--------------------------------------------------------------------
# CRON JOB SETUP EXAMPLE
#--------------------------------------------------------------------

# Add this to your crontab to process the email queue every 5 minutes:
# */5 * * * * curl -X POST "https://yourdomain.com/cron/email/process-queue?cron_token=your_secure_random_token_here" >/dev/null 2>&1

# For shared hosting (cPanel):
# Command: curl -X POST "https://yourdomain.com/cron/email/process-queue?cron_token=your_secure_random_token_here"
# Interval: Every 5 minutes

# Process larger batches less frequently (every 10 minutes, 100 emails per batch):
# */10 * * * * curl -X POST "https://yourdomain.com/cron/email/process-queue?cron_token=your_token&batch_size=100" >/dev/null 2>&1
